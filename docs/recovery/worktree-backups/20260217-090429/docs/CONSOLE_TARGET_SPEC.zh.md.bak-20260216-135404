# 大控制台目标规格（发布前冻结版）

更新时间：2026-02-14  
适用范围：`webui` 大控制台（非小控制台）

## 1. 文档目的

定义“大控制台”在本项目中的目标形态、输出规则、交互规则与验收标准。  
本规格用于统一实现方向，避免后续继续出现“日志逻辑临时拼接、重复刷屏、可读性不一致”的问题。

## 2. 角色定位

大控制台不是原始日志直出窗口，而是：

1. 面向新手用户：提供可读、可理解、可执行的“问题指引”。
2. 面向进阶用户：提供关键状态变化、请求链路、耗时与结果摘要。
3. 面向调试/排障：支持导出正式日志（原始日志）与包装日志（大控制台日志）。

## 3. 设计原则

1. 可读优先：同一条日志只表达一个核心信息。
2. 结果优先：短时中间态不刷屏，重点展示“完成/失败/状态变化”。
3. 错误可修复：错误日志必须配修复指引（可执行、简短）。
4. 去重折叠：重复错误、重复状态变更要聚合。
5. 不额外消耗额度：日志机制不得触发额外聊天/跑图真实计费请求。
6. 与小控制台分工明确：大控制台负责细节，小控制台负责摘要状态。

## 4. 非目标（本期不做）

1. 不做复杂检索语法（如 Lucene）。
2. 不做远程日志上传。
3. 不做多窗口日志共享。
4. 不在本期引入完整可视化链路图。

## 5. 数据输入与分层

大控制台消费三类输入：

1. 系统事件：启动、配置加载、窗口行为、模式切换。
2. 业务事件：聊天请求、跑图请求、桥接取图/回传、模型列表刷新。
3. 插件/桥接事件：插件心跳、队列处理、插件错误上报。

输出分两层：

1. 正式日志（Raw）：完整、细粒度、可导出调试。
2. 包装日志（Wrapped）：用户可读、折叠聚合、带修复指引。

大控制台显示包装日志，导出时支持 Raw + Wrapped 双通道。

## 6. 标准日志模型（包装日志）

每条包装日志最小字段：

```ts
type ConsoleViewLog = {
  id: string;                 // 稳定唯一 ID
  ts: string;                 // HH:mm:ss
  level: "info" | "warn" | "error";
  type: "系统" | "API" | "桥接";
  title: string;              // 简明主题
  message: string;            // 可读正文
  state?: "ok" | "running" | "failed" | "changed";
  durationMs?: number;        // 仅在有意义时显示
  requestId?: string;         // 有则保留
  guide?: string;             // 错误修复指引
  foldKey?: string;           // 重复折叠键
  repeatCount?: number;       // 折叠计数
};
```

## 7. 展示规范

### 7.1 时间与排版

1. 时间固定宽度，单行对齐。
2. 类型与正文之间固定一个空格。
3. 行距统一，避免跳行挤压。

### 7.2 颜色规则（仅三色正文）

1. 正常信息：白色正文。
2. 提示/处理中：白色正文（不再使用橙色正文）。
3. 错误：红色正文。

注：类型标签颜色可保留现有体系，但正文颜色只允许白/红。

### 7.3 运行中反馈

1. 运行中状态通过“状态动画/微动效”表现，不靠高频新增日志刷屏。
2. 短时步骤（< 1s）默认不拆成“发送中 + 成功”两条，直接输出结果条目。

## 8. 折叠与去重规则

### 8.1 重复错误折叠

同一 `foldKey`、同一错误摘要、同一修复指引，在时间窗口内聚合为一条：

1. 首次显示完整错误。
2. 后续只递增 `repeatCount`。
3. 最新时间戳更新到该聚合条目。

推荐窗口：30s（可配置）。

### 8.2 高频状态折叠

以下类型默认折叠为状态更新，不逐条新增：

1. 连通验证中间态。
2. 心跳成功。
3. 轮询正常返回（无异常）。

### 8.3 请求链路合并

同一请求链路可合并显示为一条摘要：

1. 请求成功：显示“发送成功 + 返回成功 + 耗时”。
2. 请求失败：显示“发送成功/失败 + 错误摘要 + 修复指引”。

## 9. 错误修复指引规则

### 9.1 必须包含修复指引的场景

1. 网络连接失败（DNS、超时、TLS、503/500）。
2. 限流或额度相关失败（429）。
3. 服务商地址或兼容格式不匹配。
4. 模型不可用/分组无渠道。
5. 插件未连接、端口不一致、localhost 权限问题。
6. 抓图/回传失败（无文档、无选区、编码失败）。

### 9.2 指引风格

1. 一条日志仅给一条主修复路径。
2. 使用可执行描述（“检查 A，再检查 B”），避免泛化废话。
3. 指引文字长度建议 18-48 字。

### 9.3 场景优先级（新增）

1. 先按 `scene` 匹配，再按错误关键字匹配；不得先走全局通用规则。
2. `scene=bridge` 时，桥接/插件错误优先于 API 鉴权规则。
3. 以下错误必须映射为“插件回传文件令牌问题”，不得提示 API Key：
   - `invalid file token used`
   - `import_file_token_missing`
   - `import_temp_file_path_empty`
4. 以上错误统一修复指引：
   - “Ps 回传文件令牌无效；请重试回传。若仍失败，请重载 Ps 插件后再试。”

### 9.4 `429` 专项规则（新增）

1. 日志标题统一使用：
   - “请求失败：429（额度或限流）”
2. 日志正文优先给出三类可能：
   - 当前 Key 额度不足或已欠费。
   - 短时间请求过多触发限流。
   - 分组/渠道对该模型不可用。
3. 修复指引统一为：
   - “请先检查 Key 额度与分组渠道，再降低请求频率后重试；必要时切换模型或服务商。”

## 10. 加载效果与状态语义

大控制台支持状态语义：

1. `running`：请求已发出，等待返回。
2. `ok`：流程完成。
3. `failed`：流程失败。
4. `changed`：状态变化（如插件已连接/已断开）。

加载效果仅用于 `running`；收到最终结果后必须立即切换为 `ok/failed`，不得残留。

### 10.1 标题栏双状态按钮（聊天 / 跑图）

标题栏左侧两个状态按钮（聊天状态、跑图状态）统一使用以下四色语义：

1. 绿色：服务器连接成功（当前服务可达，可发起请求）。
2. 蓝色：任务进行中（聊天请求或跑图请求正在执行）。
3. 橙色：服务器连接失败（服务不可达或连通性验证失败）。
4. 红色：任务错误（聊天或跑图请求已返回失败）。

状态优先级（同一按钮同一时刻）：

1. 红色（任务错误）最高。
2. 蓝色（任务进行中）次之。
3. 橙色（服务器连接失败）再次。
4. 绿色（服务器连接成功）默认。

备注：

1. 聊天按钮只反映聊天链路状态；跑图按钮只反映跑图链路状态。
2. 任务结束后若无错误，按钮应从蓝色恢复到绿色。

## 11. 与小控制台协同

1. 小控制台只显示简短状态词（短文案、低频变化）。
2. 大控制台提供详情、耗时、错误原因和修复指引。
3. 小控制台错误提示应引导“打开大控制台查看详情”。

## 12. 导出日志规范

导出产物包含：

1. `raw.log`：正式日志，完整事件，适合开发排查。
2. `wrapped.log`：包装日志，贴近用户看到的大控制台。

导出文件需包含：

1. 导出时间。
2. 当前版本号。
3. 服务商/兼容格式关键配置摘要（脱敏，不导出密钥明文）。

## 13. 性能要求

1. 渲染窗口内最大可视日志条数不影响交互流畅度（目标 60fps，允许短降）。
2. 高频事件必须走聚合，不得直接全量 setState 追加。
3. 折叠更新优先原地更新对象，不重复创建大数组副本。

## 14. 验收标准

### 14.1 可读性

1. 新手用户可在 10 秒内看懂“当前是否成功、失败原因、下一步操作”。
2. 同类错误连续触发时，界面不刷屏，出现“重复次数”。

### 14.2 业务闭环

1. 聊天成功：显示请求成功与耗时摘要。
2. 跑图成功：显示请求成功与耗时摘要。
3. 失败场景：有错误摘要 + 修复指引。

### 14.3 稳定性

1. 不因日志增加导致 UI 明显卡顿。
2. 切页、折叠、窗口缩放时日志区域不闪烁、不跳位。

## 15. 分阶段实施建议

### P1（先做）

1. 统一包装日志模型。
2. 去掉短时中间态刷屏。
3. 错误修复指引统一模板化。
4. 重复错误折叠。

### P2（后做）

1. 运行中状态动画统一。
2. 同请求链路合并摘要。
3. 导出 Raw/Wrapped 双日志结构化增强。

### P3（可选）

1. 日志筛选增强（按 type/level/state）。
2. 快捷定位某次请求链路。

## 16. 变更控制

本规格为当前阶段冻结目标。  
后续如需改动，按“先改文档再改代码”的顺序执行，避免再次出现实现与目标偏移。
