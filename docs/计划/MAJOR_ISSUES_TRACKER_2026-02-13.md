# Major Issues Tracker (2026-02-13)

## 已确认并执行

1. `settings` 样式收敛为单套规则  
状态：已完成  
说明：移除旧版 `settings-*` 重复规则，保留“Settings page rework”新规则段，避免参数修改被覆盖。

2. 轮询改为分层节流  
状态：已完成  
说明：改为前台快速、前台空闲较慢、后台统一慢轮询，并在 `visibilitychange` 时重新调度。

3. 遗留模型选择链路删除  
状态：已完成  
说明：移除未接线的 `pickerState` 及相关计算/监听，避免无意义监听和状态维护。

4. 会话自动命名链路移除  
状态：已完成  
说明：移除自动命名常量、校验、请求与写回逻辑；会话命名仅保留手动重命名。

5. 备份文件清理  
状态：已完成  
说明：删除 `webui/src/window_link_backup.jsx`。

## 结构重构进度

1. 组件拆分  
状态：进行中  
已做：将控制台底部区域拆分为 `webui/src/components/ConsolePanel.jsx`。  
待做：继续拆分聊天区与设置区，减少 `App.jsx` 体积和状态耦合。

2. 弹层监听合并  
状态：已完成（首轮）  
说明：模型下拉、服务商下拉、预设下拉、历史菜单、运行弹层、日志过滤、快捷配置、身份面板的外部点击/窗口变化监听已统一到单个 effect。

## 当前剩余重大项（待后续）

1. `App.jsx` 仍偏大，建议继续拆分为 `HomeView`、`SettingsView`、`MessageList` 等组件。  
2. `useEffect` 依赖项仍需二轮梳理（本次以“保持行为等价”为先，未做激进重写）。  
3. 字体资源体积较大（打包后约 7MB+），建议后续做字体子集化或按需加载。
