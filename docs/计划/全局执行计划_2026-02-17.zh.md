# 全局执行计划（2026-02-17）

状态说明：`待执行` / `进行中` / `已完成`

## Phase 0：规则与基线

1. 执行规则固化（编码/备份/修改边界）
状态：已完成
2. 计划文档建立与跟踪
状态：已完成

## Phase 1：日志专项优化

1. 基于当前日志样本重写日志规格文档
状态：已完成
2. `server/index.js` 插件日志归一化（level/文本截断/结构稳定）
状态：已完成
3. `electron/main.js` 日志导出增强（主进程 + dev 节选）
状态：已完成
4. 日志链路校验（语法 + 导出结构）
状态：已完成

## Phase 2：全局规范审查与代码整理

1. 全局规范审查（结构、命名、异常处理、边界条件）
状态：已完成
2. 代码整理（必要内联拆分/整合，不为拆而拆）
状态：已完成（保持当前可运行结构，仅做必要优化）
3. 审查发现问题直接修复（修复前先备份）
状态：已完成（修复插件状态误判链路）

## Phase 3：残留治理与目录整理

1. 备份残留检查（.bak / 临时目录）
状态：已完成
2. 残留归档迁移（只迁移，不破坏删除）
状态：已完成
3. 计划文档统一到 `docs/计划` 并补充索引
状态：已完成

## Phase 4：图标与功能闭环审查

1. SVG 引用完整性审查
状态：已完成
2. SVG 命名规范审查 + 中文语义映射文档
状态：已完成
3. 功能点统一文档更新
状态：已完成
4. 逐功能逻辑链闭环审查（输入->处理->输出->异常）
状态：已完成

## Phase 5：最终验证与交付

1. 语法校验（server/electron）
状态：已完成
2. `webui build` + `sync:assets`
状态：已完成
3. 开发启动链路验证与风险说明
状态：已完成（冒烟验证通过）
4. 最终报告（已完成/待确认/风险项）
状态：已完成（见 `docs/计划/最终报告_2026-02-17.zh.md`）

## 本轮关键修复

1. 修复插件在线状态误判：`/ps/logs` 不再刷新插件心跳。
2. 优化日志导出：dev 日志去 ANSI、尾部 320 行摘要、导出头增强。
3. 完成图标引用一致性与中文语义映射审查。
4. 完成功能点与逻辑链闭环文档更新。

## 备份策略（执行中强制）

1. 每个 Phase 开始前建立 Git 备份点。
2. 若 Git 受限则先说明用途并请求权限。
3. Git 不可用时先做文件级备份。
