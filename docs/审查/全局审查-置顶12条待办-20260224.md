# 全局审查（置顶12条待办）- 2026-02-24

1. 已复读最高守则：`是`
2. UTF-8 编码校验：`通过`

3. 审查范围
- 仓库：`D:\二二二次元\小迪助词器\prompthelper-bridge-recovered`
- 代码链路：`webui/src/App.jsx`、`electron/main.js`、`electron/server/index.js`、根/子包 `package.json`
- 过程检查：`eslint` 静态审查、`node --check` 语法检查、`validate:p7` 验收脚本

4. 不变式清单
- 悬浮层定位链路必须“单一口径 + 单一权威源”，禁止混合口径导致抖动。
- 用户可见文案必须 UTF-8 可读，禁止乱码兜底文案进入运行态。
- 服务端错误必须可观测，禁止静默吞错。
- 发布链路必须有可复现的质量门禁（lint/test/build），禁止仅靠人工回归。

5. 发现问题（置顶12条待办）

| 排序 | 等级 | 待办事项 | 根因证据（文件+行号） | 影响范围 | 最小根治建议（不实现） |
|---|---|---|---|---|---|
| 1 | 阻断 | 修复“画布”默认文案乱码 | `webui/src/App.jsx:8861` | 导入目标弹窗回退名展示异常 | 统一替换为正确 UTF-8 文案并做全文乱码扫描门禁 |
| 2 | 高 | 修复上传区横向滚动 effect 的依赖缺失 | `webui/src/App.jsx:31389` | hover 抑制/恢复逻辑可能使用陈旧闭包 | 补齐依赖或改为稳定 ref 回调 |
| 3 | 高 | 修复启动最小尺寸 effect 的依赖缺失 | `webui/src/App.jsx:34983` | 启动后尺寸基线可能与当前函数实现不一致 | 补齐 `waitAnimationFrames` 依赖或内联稳定实现 |
| 4 | 高 | 清理生产态 TLS 证书绕过策略 | `electron/main.js:3790-3800` | 特定域名可绕过证书错误，存在传输信任风险 | 改为可配置、默认关闭，并增加显式告警与开关审计 |
| 5 | 高 | 收紧 Bridge 服务 CORS 策略 | `electron/server/index.js:17` | 本地端口被任意来源跨域调用风险 | 按白名单 origin 校验并限制 method/header |
| 6 | 高 | 去除请求热路径中的同步 I/O | `electron/server/index.js:622,629,672,708` | 大文件读写会阻塞事件循环，影响接口实时性 | 迁移为 async fs + 队列化写入 |
| 7 | 高 | 移除关键链路静默吞错 | `electron/server/index.js:122,162,168` | SSE/队列返回失败不可见，定位困难 | catch 中至少打结构化日志并记录上下文 |
| 8 | 高 | 建立统一质量门禁脚本（lint/test） | `package.json:5-16`、`webui/package.json:6-12` | 发布前缺少统一 lint/test 入口，回归成本高 | 在根脚本新增 `lint`、`test`、`check` 聚合命令 |
| 9 | 中 | 清理未使用配置变量 | `webui/src/App.jsx:380` | 增加维护噪音，降低可读性 | 删除 `chatRecommendedModelCatalog` 或补齐使用链路 |
| 10 | 中 | 清理未使用图标变量 | `webui/src/App.jsx:554` | 图标资产责任边界不清晰 | 删除 `iconChatTitleHistory` 或接入真实使用点 |
| 11 | 中 | 清理未使用模型函数 | `webui/src/App.jsx:10466,10476` | 模型能力链路存在死代码 | 删除 `getChatPresetModels/getImagePresetModels` 或补齐调用 |
| 12 | 中 | 清理未使用参数与动作上下文 | `webui/src/App.jsx:19592,26238` | 保存/日志动作接口签名冗余 | 删除 `preferredActiveSessionIdInput` 与 `_logEntry` 或落地用途 |

6. 风险与回归关注点
- 当前工作区变更量极大（`git status --short` 统计：总 685，删除 643，未跟踪 21，修改 21），发布前必须先做“结构性提交切分 + 回归基线”。
- `webui/src/App.jsx` 单文件体量过大（约 36851 行），后续改动建议先做模块拆分，否则定位与回归风险持续升高。

7. 下一轮审查入口
- 先做“待办 1~8”的风险清零评审（尤其 2~7 的运行态风险）。
- 然后做“待办 9~12”的清洁提交，确保 lint 归零。
- 最后再进入“按批次提交计划”的发版前验收（与 `版本管理表格与GitHub提交计划.md` 对齐）。
